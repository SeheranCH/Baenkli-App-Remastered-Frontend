import React from "react";

const Title = ({ text }) => {
    return (
        <p style={{textAlign: "center"}}>
            <b>
                {text}
            </b>
        </p>
    )
};
export default Title;